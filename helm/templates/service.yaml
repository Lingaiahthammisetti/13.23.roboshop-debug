apiVersion: v1
kind: Service
metadata:
  name: debug
  namespace: roboshop
spec:
  selector:
    app: debug
    project: roboshop
  ports:
  - protocol: TCP
    port: 5672 #service-port
    targetPort: 5672 #container-port